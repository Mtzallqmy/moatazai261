# Google OAuth setup

In Google Cloud Console create a Web OAuth client. Use the Supabase provider callback shown in the Supabase dashboard as the authorized redirect URI. Add localhost, approved preview/production hosts, and the custom host as JavaScript origins where applicable. Enter the client ID and secret only in Supabase Auth provider settings, never in this repository.
