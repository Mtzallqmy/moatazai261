import { z } from "zod";
export const providerTypes=z.enum(["openai","anthropic","google-gemini","openrouter","groq","mistral","cohere","deepseek","xai","azure-openai","amazon-bedrock","openai-compatible"]);
export const providerInputSchema=z.object({name:z.string().trim().min(2).max(100),slug:z.string().regex(/^[a-z0-9][a-z0-9-]{1,62}$/),providerType:providerTypes,baseUrl:z.url(),authType:z.enum(["bearer","api_key_header","query","basic","custom_headers","oauth_client_credentials","none"]).default("bearer"),chatEndpoint:z.string().max(200).default("chat/completions"),modelsEndpoint:z.string().max(200).default("models"),apiVersion:z.string().max(50).optional(),timeoutMs:z.number().int().min(1000).max(600000).default(90000),retryCount:z.number().int().min(0).max(5).default(2),priority:z.number().int().min(0).max(10000).default(100),enabled:z.boolean().default(false),configuration:z.record(z.string(),z.unknown()).default({}),credential:z.object({secret:z.string().min(4).max(20_000),headerName:z.string().max(100).optional(),username:z.string().max(200).optional()}).optional()});
export const modelInputSchema=z.object({providerId:z.uuid(),modelKey:z.string().trim().min(1).max(200),modelAlias:z.string().regex(/^[a-z0-9][a-z0-9._-]{1,100}$/).optional(),displayName:z.string().trim().min(1).max(120),description:z.string().max(1000).optional(),enabled:z.boolean().default(false),visibleToUsers:z.boolean().default(true),isDefault:z.boolean().default(false),contextWindow:z.number().int().positive().optional(),maxOutputTokens:z.number().int().positive().optional(),maxMessageChars:z.number().int().positive().default(100000),inputModalities:z.array(z.enum(["text","image","audio","video","document"])).min(1),outputModalities:z.array(z.enum(["text","image","audio"])).min(1),capabilities:z.object({streaming:z.boolean(),tools:z.boolean(),vision:z.boolean(),audio:z.boolean(),video:z.boolean(),documents:z.boolean(),embeddings:z.boolean(),structuredOutput:z.boolean()}),pricingMetadata:z.record(z.string(),z.number().nonnegative()).default({}),billingTier:z.enum(["free","paid","premium"]).default("paid"),releaseStage:z.enum(["stable","beta"]).default("stable")});
