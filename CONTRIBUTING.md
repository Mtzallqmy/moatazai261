# Contributing

Create a focused branch, keep UI, services, repositories, schemas, and provider adapters separated, and include migrations for every database change. Never commit `.env` files or credentials. Before requesting review, run lint, type-check, tests, and the production build. Security-sensitive changes require tests for authorization failure as well as success.
