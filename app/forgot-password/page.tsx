import { PasswordRecoveryForm } from "@/components/password-recovery-form";import { SiteHeader } from "@/components/site-header";import { requireSupabasePublicEnv } from "@/config/env";
export default function Page(){const env=requireSupabasePublicEnv();return <div className="site-page"><SiteHeader/><main className="page-shell"><section className="auth-card"><h1>استعادة كلمة المرور</h1><p>سنرسل رابطًا آمنًا إذا كان البريد مسجلًا.</p><PasswordRecoveryForm configuration={{url:env.NEXT_PUBLIC_SUPABASE_URL,publishableKey:env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY}}/></section></main></div>;}
