import { requirePermission } from "@/lib/auth/guards";
import { createAdminClient } from "@/lib/supabase/admin";
export default async function Page(){await requirePermission("agent_runs.read");const{data}=await createAdminClient().from("background_jobs").select("id,job_type,resource_type,status,progress,attempts,error_code,created_at").order("created_at",{ascending:false}).limit(100);return <section><h1>المهام الخلفية</h1><p>صف انتظار دائم لمعالجة الملفات والأبحاث؛ لا يدّعي وجود عامل إذا لم يكن متصلًا.</p><div className="data-table">{(data??[]).map(job=><div className="table-row" key={job.id}><span>{job.job_type}<small>{job.resource_type}</small></span><span>{job.status}</span><span>{job.progress}% · محاولة {job.attempts}</span><span>{job.error_code??new Date(job.created_at).toLocaleString("ar")}</span></div>)}{!data?.length&&<p className="empty-state">لا توجد مهام مسجلة.</p>}</div></section>;}
