"use client";
import { useState } from "react";import { createSupabaseBrowserClient,type SupabaseBrowserConfiguration } from "@/lib/supabase/browser";
export function PasswordRecoveryForm({configuration}:{configuration:SupabaseBrowserConfiguration}){const [email,setEmail]=useState("");const [message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();await createSupabaseBrowserClient(configuration).auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/callback?next=/account/update-password`});setMessage("إذا كان البريد مسجلاً فستصلك رسالة الاستعادة.");}return <form className="password-form" onSubmit={submit}><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="البريد الإلكتروني"/><button className="button primary">إرسال رابط الاستعادة</button>{message&&<p className="notice">{message}</p>}</form>;}
export function PasswordUpdateForm({configuration}:{configuration:SupabaseBrowserConfiguration}){const [password,setPassword]=useState("");const [message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const {error}=await createSupabaseBrowserClient(configuration).auth.updateUser({password});setMessage(error?"تعذر تحديث كلمة المرور.":"تم تحديث كلمة المرور.");}return <form className="password-form" onSubmit={submit}><input type="password" minLength={10} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="كلمة المرور الجديدة"/><button className="button primary">تحديث كلمة المرور</button>{message&&<p className="notice">{message}</p>}</form>;}
